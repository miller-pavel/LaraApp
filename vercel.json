{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": { "outputDirectory": "public/build" },
            "buildCommand": "next build",
            "cleanUrls": true
        }
    ]
}
